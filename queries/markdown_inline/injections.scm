; Intentionally empty (no `; extends`) — overrides nvim-treesitter's
; markdown_inline injections.scm to avoid the Neovim 0.12 treesitter crash.
; See queries/markdown/injections.scm for the full explanation.
